export enum AppRoutes {
  Login = '/login',
  Gallery = '/gallery',
  Listings = '/listings',
}

export enum PageNameEnums {
  Gallery = 'gallery',
  Listings = 'listings',
}

export enum ErrorCodes {
  NETWORK_ERROR = 'NETWORK_ERROR',
  GENERIC_ERROR = 'GENERIC_ERROR',
}